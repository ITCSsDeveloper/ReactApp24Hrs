import './PhotoPreview.css'

function PhotoPreview(props) {
    const { data } = props;
    return (
        <div>
            <h1>SHow Preview</h1>
        </div>
    );
}

export default PhotoPreview;